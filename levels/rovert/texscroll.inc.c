void scroll_rovert_dl_cloudpuff_mesh_layer_5_vtx_0() {
	int i = 0;
	int count = 25;
	int width = 64 * 0x20;
	int height = 64 * 0x20;

	static int currentX = 0;
	int deltaX;
	Vtx *vertices = segmented_to_virtual(rovert_dl_cloudpuff_mesh_layer_5_vtx_0);

	deltaX = (int)(0.06999999284744263 * 0x20) % width;

	if (absi(currentX) > width) {
		deltaX -= (int)(absi(currentX) / width) * width * signum_positive(deltaX);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[0] += deltaX;
	}
	currentX += deltaX;
}

void scroll_rovert_dl_cloudpuff_001_mesh_layer_5_vtx_0() {
	int i = 0;
	int count = 25;
	int width = 64 * 0x20;
	int height = 64 * 0x20;

	static int currentX = 0;
	int deltaX;
	Vtx *vertices = segmented_to_virtual(rovert_dl_cloudpuff_001_mesh_layer_5_vtx_0);

	deltaX = (int)(0.06999999284744263 * 0x20) % width;

	if (absi(currentX) > width) {
		deltaX -= (int)(absi(currentX) / width) * width * signum_positive(deltaX);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[0] += deltaX;
	}
	currentX += deltaX;
}

void scroll_rovert_dl_cloudpuff_002_mesh_layer_5_vtx_0() {
	int i = 0;
	int count = 25;
	int width = 64 * 0x20;
	int height = 64 * 0x20;

	static int currentX = 0;
	int deltaX;
	Vtx *vertices = segmented_to_virtual(rovert_dl_cloudpuff_002_mesh_layer_5_vtx_0);

	deltaX = (int)(0.06999999284744263 * 0x20) % width;

	if (absi(currentX) > width) {
		deltaX -= (int)(absi(currentX) / width) * width * signum_positive(deltaX);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[0] += deltaX;
	}
	currentX += deltaX;
}

void scroll_rovert_dl_cloudpuff_003_mesh_layer_5_vtx_0() {
	int i = 0;
	int count = 25;
	int width = 64 * 0x20;
	int height = 64 * 0x20;

	static int currentX = 0;
	int deltaX;
	Vtx *vertices = segmented_to_virtual(rovert_dl_cloudpuff_003_mesh_layer_5_vtx_0);

	deltaX = (int)(0.06999999284744263 * 0x20) % width;

	if (absi(currentX) > width) {
		deltaX -= (int)(absi(currentX) / width) * width * signum_positive(deltaX);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[0] += deltaX;
	}
	currentX += deltaX;
}

void scroll_rovert_dl_cloudpuff_004_mesh_layer_5_vtx_0() {
	int i = 0;
	int count = 25;
	int width = 64 * 0x20;
	int height = 64 * 0x20;

	static int currentX = 0;
	int deltaX;
	Vtx *vertices = segmented_to_virtual(rovert_dl_cloudpuff_004_mesh_layer_5_vtx_0);

	deltaX = (int)(0.06999999284744263 * 0x20) % width;

	if (absi(currentX) > width) {
		deltaX -= (int)(absi(currentX) / width) * width * signum_positive(deltaX);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[0] += deltaX;
	}
	currentX += deltaX;
}

void scroll_rovert_dl_cloudpuff_005_mesh_layer_5_vtx_0() {
	int i = 0;
	int count = 25;
	int width = 64 * 0x20;
	int height = 64 * 0x20;

	static int currentX = 0;
	int deltaX;
	Vtx *vertices = segmented_to_virtual(rovert_dl_cloudpuff_005_mesh_layer_5_vtx_0);

	deltaX = (int)(0.06999999284744263 * 0x20) % width;

	if (absi(currentX) > width) {
		deltaX -= (int)(absi(currentX) / width) * width * signum_positive(deltaX);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[0] += deltaX;
	}
	currentX += deltaX;
}

void scroll_rovert_dl_cloudpuff_006_mesh_layer_5_vtx_0() {
	int i = 0;
	int count = 25;
	int width = 64 * 0x20;
	int height = 64 * 0x20;

	static int currentX = 0;
	int deltaX;
	Vtx *vertices = segmented_to_virtual(rovert_dl_cloudpuff_006_mesh_layer_5_vtx_0);

	deltaX = (int)(0.06999999284744263 * 0x20) % width;

	if (absi(currentX) > width) {
		deltaX -= (int)(absi(currentX) / width) * width * signum_positive(deltaX);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[0] += deltaX;
	}
	currentX += deltaX;
}

void scroll_rovert_dl_cloudpuff_007_mesh_layer_5_vtx_0() {
	int i = 0;
	int count = 25;
	int width = 64 * 0x20;
	int height = 64 * 0x20;

	static int currentX = 0;
	int deltaX;
	Vtx *vertices = segmented_to_virtual(rovert_dl_cloudpuff_007_mesh_layer_5_vtx_0);

	deltaX = (int)(0.06999999284744263 * 0x20) % width;

	if (absi(currentX) > width) {
		deltaX -= (int)(absi(currentX) / width) * width * signum_positive(deltaX);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[0] += deltaX;
	}
	currentX += deltaX;
}

void scroll_rovert_dl_cloudpuff_008_mesh_layer_5_vtx_0() {
	int i = 0;
	int count = 25;
	int width = 64 * 0x20;
	int height = 64 * 0x20;

	static int currentX = 0;
	int deltaX;
	Vtx *vertices = segmented_to_virtual(rovert_dl_cloudpuff_008_mesh_layer_5_vtx_0);

	deltaX = (int)(0.06999999284744263 * 0x20) % width;

	if (absi(currentX) > width) {
		deltaX -= (int)(absi(currentX) / width) * width * signum_positive(deltaX);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[0] += deltaX;
	}
	currentX += deltaX;
}

void scroll_rovert_dl_cloudpuff_009_mesh_layer_5_vtx_0() {
	int i = 0;
	int count = 25;
	int width = 64 * 0x20;
	int height = 64 * 0x20;

	static int currentX = 0;
	int deltaX;
	Vtx *vertices = segmented_to_virtual(rovert_dl_cloudpuff_009_mesh_layer_5_vtx_0);

	deltaX = (int)(0.06999999284744263 * 0x20) % width;

	if (absi(currentX) > width) {
		deltaX -= (int)(absi(currentX) / width) * width * signum_positive(deltaX);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[0] += deltaX;
	}
	currentX += deltaX;
}

void scroll_rovert() {
	scroll_rovert_dl_cloudpuff_mesh_layer_5_vtx_0();
	scroll_rovert_dl_cloudpuff_001_mesh_layer_5_vtx_0();
	scroll_rovert_dl_cloudpuff_002_mesh_layer_5_vtx_0();
	scroll_rovert_dl_cloudpuff_003_mesh_layer_5_vtx_0();
	scroll_rovert_dl_cloudpuff_004_mesh_layer_5_vtx_0();
	scroll_rovert_dl_cloudpuff_005_mesh_layer_5_vtx_0();
	scroll_rovert_dl_cloudpuff_006_mesh_layer_5_vtx_0();
	scroll_rovert_dl_cloudpuff_007_mesh_layer_5_vtx_0();
	scroll_rovert_dl_cloudpuff_008_mesh_layer_5_vtx_0();
	scroll_rovert_dl_cloudpuff_009_mesh_layer_5_vtx_0();
}
